import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pathtojannah.app',
  appName: 'The Path to Jannah',
  webDir: 'dist'
};

export default config;
